[build]
  publish = "."
  functions = "netlify/functions"

# If you need SPA routing, keep it LAST and DO NOT force it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
