[build]
  command = "npm run build"      # this runs your build step
  publish = "."                  # your index.html is in the root folder
  functions = "netlify/functions"

# This makes it a single-page app (SPA) so chat works on every URL
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
